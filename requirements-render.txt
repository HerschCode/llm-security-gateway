# Dependency set for the Render free tier (512MB RAM). No torch: the FULL
# 3-layer ensemble still runs, because layer 3 (the scratch classifier) is
# served by gateway/detectors/classifier_numpy.py -- a torch-free
# re-implementation of the trained model's forward pass (bit-parity verified
# in tests/test_classifier_numpy_parity.py), not a reduced ensemble. torch is
# only needed to TRAIN that model (scripts/train_scratch_classifier.py), which
# happens offline, not in this deployed image. See docs/decisions.md.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
httpx==0.28.1
scikit-learn>=1.7,<1.9
scipy==1.17.1
numpy==2.2.6
joblib==1.6.0
pyyaml==6.0.3
