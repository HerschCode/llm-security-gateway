# Full / local / dev dependencies. Target: Python 3.12 (the committed model
# artifacts were fit under scikit-learn 1.8 on 3.12; on 3.10/3.11 you'll get a
# cosmetic InconsistentVersionWarning on load -- harmless, see docs/decisions.md).
#
# For the 512MB Render free-tier deploy, use requirements-render.txt instead --
# it's the serving-only subset (no torch: layer 3 is served by
# gateway/detectors/classifier_numpy.py, not the torch model). torch here is
# for TRAINING (scripts/train_scratch_classifier.py) and for running
# gateway/detectors/classifier.py in scripts/evaluate.py / tests, not for
# what actually gets deployed.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
httpx==0.28.1
scikit-learn>=1.7,<1.9
scipy==1.15.3
numpy==2.2.6
joblib==1.6.0
torch==2.14.0
pyyaml==6.0.3
pytest==9.1.1

# NOT required to run anything else in this repo -- only needed for
# scripts/train_distilbert_finetune.py, which needs network access to
# huggingface.co (works from this environment as of 2026-09-11; see
# docs/decisions.md for the environment where it didn't). Left commented so a
# plain `pip install -r requirements.txt` doesn't fetch ~260MB of weights
# nobody asked for.
# transformers>=4.40
# datasets>=2.19
# accelerate>=1.1.0
